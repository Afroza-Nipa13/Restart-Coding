const tax = 5000;
let eta = 5;
eta = 2


const student = { name : 'nayeem', marks: 50}
const friends = [ 'nipa', 'liza', 'khadiza', 'masoud', 'sarwar'
]

// default parameter

function add(num1, num2 = 0){

}

// template string

const dynamicText = `My Tax: ${tax} and marks ${student.marks + 2} has friend : ${friends[3]}`;

console.log(dynamicText)
